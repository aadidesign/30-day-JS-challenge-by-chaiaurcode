
// Task 1: Match all occurrences of "JavaScript" in a string
const jsMatch = (str) => str.match(/JavaScript/g);

// Task 2: Match all digits in a string
const digitMatch = (str) => str.match(/\d+/g);
