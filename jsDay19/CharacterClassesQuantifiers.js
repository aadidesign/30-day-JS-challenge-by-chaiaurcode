
// Task 3: Match all words starting with a capital letter
const capitalWordMatch = (str) => str.match(/\b[A-Z][a-z]*\b/g);

// Task 4: Match all sequences of one or more digits
const digitSequencesMatch = (str) => str.match(/\d+/g);
